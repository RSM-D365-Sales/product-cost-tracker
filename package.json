{
  "name": "po-receipt-tracker",
  "private": true,
  "version": "0.1.0",
  "description": "Product Cost Inquiry — D365 F&SCM-styled purchase receipt landed cost tracker",
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "dev:server": "npm run dev --workspace server",
    "dev:all": "concurrently -n web,api -c cyan,magenta \"npm:dev\" \"npm:dev:server\"",
    "build": "npm run build --workspace web",
    "typecheck": "npm run typecheck --workspace web && npm run typecheck --workspace server",
    "preview": "npm run preview --workspace web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "playwright": "^1.62.0"
  }
}
